Q:-  Print numbers from N to 1 (space separated) without the help of loops ?

Solution:-

#include<iostream>
using namespace std;
int cnt = 1;
    void printNos(int N) {
        // code here
        if(N==0){
            return ;
        }
        cout<<N<<" ";
        printNos(N-1);
    }
    int main(){
        int N;
        cin>>N;
        printNos(N);
        return 0;
    }
